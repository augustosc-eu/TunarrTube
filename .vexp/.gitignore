# vexp - internal files (auto-generated, not user-managed)
# manifest.json and index.db are local index state, rewritten on nearly every
# vexp query; untracked so they stop showing up as a perpetual pending change
manifest.json
index.db
index.db-wal
index.db-shm
index.lock
vexp.log*
daemon.log*
daemon.sock
daemon.pid
daemon.pipe
mcp.port
healthy
start-blocked
stop-gate-*.done
idle-gate-*.done
task-*.txt
coverage.json
edit-hints/
search-hints/
read-hints/
parent_workspace.json
token-savings-report.md
daemon-job-warning
mcp.token
backups/
