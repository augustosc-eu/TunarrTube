DATABASE_URL="file:./ytarr.db"
# Optional overrides. TunarrTube otherwise discovers both programs dynamically.
# TUNARRTUBE_YTDLP_PATH="/opt/homebrew/bin/yt-dlp"
# TUNARRTUBE_FFMPEG_PATH="/opt/homebrew/bin/ffmpeg"
# TUNARRTUBE_MEDIA_DIR="/absolute/path/to/media"
# TUNARRTUBE_TUNARR_URL="http://127.0.0.1:8000"
# TUNARRTUBE_THUMBNAIL_DIR="/absolute/path/to/thumbnails"
# Absolute path to a Netscape-format cookies.txt so yt-dlp can authenticate for age-restricted/bot-checked
# videos ("Sign in to confirm your age/you're not a bot"). Optional -- see README's "Age-restricted or
# sign-in-required videos". Mount the file into the container read-only rather than pointing this at a
# host path the container can't see.
# TUNARRTUBE_YTDLP_COOKIES="/config/cookies.txt"
# How many lightweight jobs (metadata, thumbnail, sync, tunarr_*, etc.) run concurrently. Media fetches
# and CPU-heavy renders have the separate limits below.
# TUNARRTUBE_GENERAL_WORKERS=3
# Shared media-fetch concurrency across downloads, cache fills, playback, and Tunarr materialization.
# TUNARRTUBE_DOWNLOAD_CONCURRENCY=1
# FFmpeg encoder/filter threads per render. The render lane itself runs one job at a time.
# TUNARRTUBE_RENDER_THREADS=2

# AI Programming (optional): set one or both to enable the "AI Programming" order on a Source/Channel.
# ANTHROPIC_API_KEY="sk-ant-..."
# OPENAI_API_KEY="sk-..."
# TUNARRTUBE_OPENAI_MODEL="gpt-4o"

# Docker Compose host settings (not read by the TunarrTube process directly).
# TUNARRTUBE_PORT=3000
# TUNARR_PORT=8000
# TZ=UTC
# TUNARR_IMAGE=chrisbenincasa/tunarr:latest

# Existing YTARR_* variables remain supported as legacy aliases.
